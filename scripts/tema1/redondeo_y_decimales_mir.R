#N cifras significativas de un número
print(sqrt(2),6)

#redondear a ciertas cifras significativas
round(sqrt(2),3)

#Parte entera por defecto (redondea hacia abajo)
floor(sqrt(2))

#Rendondear hacia arriba
ceiling(sqrt(2))

#Truncamiento
trunc(sqrt(2))

#Ver 15 cifras del número sin exponencial
print(2^50, 15)

round(sqrt(2))
round(sqrt(2),0)