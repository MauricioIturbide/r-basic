#Definición de una variable
x = (pi^2)/2 #sintaxis con igual
y <- cos(pi/4) #sintaxis con flecha
sin(pi/4) + cos(pi/4) -> z #  Otro tipo de sintaxis con flecha

edad <- 24
nombre <- 'Mauricio'

pi.4 = 4*pi